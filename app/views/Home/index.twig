<!doctype html>
<html>
    <head>
        <meta charset="utf-8"/>
        <title></title>
    </head>
    <body>
        mi MVC!! en twig
                            otra cosa
        <ul>
            {% for record in records %}
                <li>{{ record.username }}
                    {{ record.id }}
                    {{ record.email }}</li>
            {% endfor %}
        </ul>
    </body>
</html>